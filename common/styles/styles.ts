import {StyleSheet} from 'react-native';

export const commonAlignment = StyleSheet.create({
  centerAligned: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
